# AGENTS.md

## 기본 작업 원칙

- 사용자가 `수정해줘`, `구현해줘`, `해결해줘`처럼 실행을 요청하면 계획만 제시하고 멈추지 말고 구현, 검증, 커밋, 푸시까지 진행한다.
- 작업 시작 전에 `git status --short`로 기존 변경을 확인하고, 내가 만든 변경과 다른 세션/사용자 변경을 구분한다.
- 다른 세션에서 생긴 변경이 있으면 되돌리지 않는다. 사용자가 “전체 커밋/푸시”를 요청한 경우에만 함께 포함하고, 그 외에는 관련 변경만 커밋한다.
- 작업 중 필요한 사실은 먼저 저장소에서 확인한다. 파일 위치, 스크립트, 기존 패턴처럼 확인 가능한 내용을 사용자에게 다시 묻지 않는다.

## 변경 범위별 체크리스트

- 주요 계산식, 입력 항목, 계산 모드, 금액 표시 기준이 바뀌면 앱 도움말과 `README.md`를 함께 업데이트한다.
- 계산 로직 변경 시 `src/fireCalculator.ts`와 `src/fireCalculator.test.ts`를 함께 점검하고, 정상값뿐 아니라 경계값, 보정값, 도달 실패 케이스를 확인한다.
- 금액, 비율, 연도 입력을 바꾸면 모바일 키보드, 스크린리더 설명, 키보드 조작 가능성, 잘못된 입력 보정 방식을 같이 확인한다.
- 공유 가능한 화면 상태를 추가하거나 바꾸면 URL 쿼리 파라미터, 새로고침 복원, 기본값 fallback을 함께 점검한다.
- SEO 가이드, 메타데이터, sitemap, 정적 페이지 생성 흐름을 바꾸면 `npm run build`로 `scripts/generate-seo-pages.mjs`까지 통과하는지 확인한다.

## 프론트엔드/UI 작업

- 프론트엔드 UI/UX 작업 시 `frontend-design`, `vercel-react-best-practices`, `web-design-guidelines` Skills를 먼저 참고한다.
- React 변경 시 불필요한 재렌더링, 무거운 파생 계산, 큰 번들로 이어지는 import를 함께 점검한다.
- UI 변경 후에는 Playwright Skills로 데스크톱과 모바일 뷰포트에서 레이아웃, 포커스 이동, 주요 상호작용을 확인한다.
- 모바일 툴팁, 그래프, 제목 줄바꿈처럼 이전에 반복 수정된 영역은 실제 좁은 뷰포트에서 잘림, 넘침, 지나친 축소가 없는지 확인한다.
- UI 문구를 바꾸면 앱 화면, 도움말, README, SEO 메타 문구 사이의 용어가 어긋나지 않는지 확인한다.

## 테스트와 검증

- Test 시에는 Playwright Skills를 활용한다.
- 계산 로직 변경 후에는 `npm test`를 실행한다.
- 타입, 빌드, SEO 생성에 영향이 있으면 `npm run build`를 실행한다.
- 로컬 서버가 필요하면 `npm run dev -- --host 127.0.0.1`을 우선 사용하고, 포트 충돌이 있으면 다른 포트를 지정한다.
- 검증을 실행하지 못했거나 일부만 확인했다면 최종 요약에 그 이유와 남은 위험을 한국어로 명시한다.

## 커밋과 푸시

- 작업이 끝나면 즉시 `git commit`과 `git push`까지 진행한다.
- 커밋 전 `git status --short`와 필요한 diff를 확인해 의도한 파일만 포함됐는지 점검한다.
- 커밋 메시지는 한국어로 작성한다.
- 커밋 설명과 작업 요약도 한국어로 작성한다.
- 단순 수정이라도 커밋 메시지는 변경 의도를 드러내게 작성한다.
- 푸시 실패 시 원인을 확인하고, 해결 가능한 인증/원격/동기화 문제는 처리한 뒤 다시 푸시한다.

## 마무리 보고

- 최종 응답은 한국어로 작성한다.
- 작업 요약, 실행한 검증, 커밋 해시, 푸시 여부를 간단히 포함한다.
- README나 도움말을 업데이트하지 않은 경우, 계산식/입력/표시 기준 변경이 아니어서 제외했다는 식으로 이유를 명확히 한다.
